from main import *

print("running rpeditor\n")
main()
        
# get a variable to track how many messages there are first
# then extract name and message
# if next line is \n (end of message) then add message, then set variables to empty
# at the start of the loop, first check of Name and Message are set to the "empty" ones first 

# start the loop defining a Message object, add to variable counter until you see a blank line
# then write Message into text file (and add 4 \n's) and then reset Message object